apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-boot-generic.fullname" . }}
  labels:
    {{- include "spring-boot-generic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-boot-generic.selectorLabels" . | nindent 4 }}
